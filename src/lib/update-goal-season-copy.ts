/**
 * Copy for Update Goal when same_season_sync is unavailable (no active season row).
 * Client + API safe — no server imports.
 */
export const UPDATE_GOAL_REQUIRES_NEW_CHAPTER_USER_MESSAGE =
  "Your account needs a new chapter before this goal can be updated. Your past proof stays safe.";
